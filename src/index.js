import React from 'react';
import ReactDOM from 'react-dom';
import App from "../../text-editor/src/App"

ReactDOM.render(
  <React.StrictMode>
    <App></App>
  </React.StrictMode>,
  document.getElementById('root')
);
